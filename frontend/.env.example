# Optional: backend URL when the UI is hosted separately (e.g. Vercel → API on Railway).
# Leave empty for local dev (Vite proxies /api to localhost:3001).
# VITE_API_URL=https://your-backend.example.com
